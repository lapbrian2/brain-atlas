/**
 * NeuralPulse — DEPRECATED.
 * The point-cloud/node-based brain no longer has a cortical surface to pulse.
 * Pulsing is now handled per-node in BrainModel.tsx.
 * Kept as empty component to avoid broken imports.
 */
export default function NeuralPulse() {
  return null
}
